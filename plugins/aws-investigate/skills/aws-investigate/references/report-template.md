# 產出報告

報告目標讀者涵蓋前端、後端、Tech Lead、SRE、TPM、主管，乃至外部協作團隊，各方對系統背景的了解程度不一。報告的目的是讓多方**一次讀懂來龍去脈並知道下一步**，不是交代責任。因此：
- 技術細節（程式碼、查詢語法）可以簡單解釋，不用預設讀者一定懂
- 系統架構說明是幫讀者找出「這東西在哪」，不是在界定誰該負責
- 程式碼位置標籤不加「後端同仁參考」之類的觀眾限定語；任何需要了解的人都可以看

此模板是內建事件報告格式。若專案有固定欄位、嚴重度定義、issue tracker 或 owner 標記方式，沿用實際慣例。

**每個數據點必須附查詢來源編號（見 `references/analysis-principles.md` 的「數據點附查詢來源」章節）。**

輸出路徑：`reports/YYYY-MM-DDTHHMM.md`，存放於專案根目錄的 `reports/` 目錄。

圖表路徑：`reports/assets/YYYY-MM-DDTHHMM/`，與報告檔名對應。產圖方式見 `references/metrics-charts.md`。

報告有兩種類型，依入口和情境選擇：
- **定期掃描報告**（入口 A）：週報 / 健檢，3-30-300 分層 + 收折式技術細節
- **事件報告**（入口 B / 特定問題調查）：四層 Progressive Disclosure（Executive Card → 事件總覽 → 跨團隊影響 → 技術細節），主管 30 秒掌握結論、Tech Lead 5 分鐘了解全貌、工程師讀完整技術脈絡

---

## 通用撰寫原則（兩種報告都適用）

### 數據與來源

- 每個數據點附查詢來源編號（`[Q1]`、`[ALB-Q1]` 等），讓讀者能自行驗證（詳見 `analysis-principles.md`「數據點附查詢來源」）
- 查詢來源編號只出現在 `<details>` 技術細節區塊內，主文不放——會議投影時這些編號對多數讀者是雜訊
- 不同觀測層的數據標明來源：後端 application log ≠ CDN access log ≠ ALB access log，不混在同一行
- 語言精準：資料有混合結果時用限定語，不用絕對語句（詳見 `analysis-principles.md`「語言精準」）
- 行動清單每行有優先序 + 建議負責方 + 對應 issue（指回上方 issue 段落標題，技術細節由 issue 段落 SSOT）；負責方標「團隊/角色」而非個人名字
- 報告產出後不預期有人回來更新內容。不確定的資訊（如無法從 log 判斷的負責方）用 `❓` 標記讓讀者一眼看出需要在會議上釐清
- 涉及基礎設施指標的問題，附 CloudWatch 圖表（PNG）讓讀者直觀看到異常趨勢；正常指標文字帶過即可，不需要附圖
- 敏感資訊（token、secret）已遮蔽

### 寫作風格

報告的目的是讓讀者快速吸收和行動。以下原則基於 NNG（Nielsen Norman Group）掃描閱讀研究、Cognitive Load Theory、Google Developer Documentation Style Guide，以及 Blameless Postmortem 文化共識。

**語氣定位——Knowledgeable Friend（Google Style Guide）**：像一個懂技術的同事在跟主管和團隊說明狀況。清楚、直接、容易吸收，但用語得體。

| 面向 | 做 | 不做 |
| --- | --- | --- |
| 語氣 | 專業白話（Professional Plain Language）：清楚直接，讀者不用想就能理解 | 書面腔（「使用者面向影響有限」）或輕挑口語（「塞爆了」「它幹了什麼」） |
| 表達形式 | 條列、表格為主，段落為輔——NNG 研究顯示 79% 讀者掃描而非逐字閱讀，結構化格式降低 extraneous cognitive load | 整段散文描述（wall of text）——迫使讀者花認知資源在解碼排版而非理解內容 |
| 精簡 | As concise as possible, but as wordy as necessary——用剛好夠的字讓人不用猜意思 | 砍到看不懂（過度精簡），或加字到囉嗦（冗餘） |
| 態度 | Blameless：焦點放在系統和流程，不放在個人。「How did the system allow this?」 | 批評個人或團隊，使用輕挑或嘲諷語氣 |
| 重複 | DRY（Don't Repeat Yourself）：同一個 fact 只出現在一個 section | 同一件事在多個 section 用不同措辭重述 |
| 未決策事項 | 只放「事實」和「已決定的事」。Action Items 是 AI 基於調查的初步建議（Proposed），明確標示需團隊討論確認 | 把建議寫成定案（如「P1 下週完成」），或替團隊做決策 |

### 標題與 Emoji 慣例

每個 issue 的標題用 **{主題emoji} + 一句白話摘要** 作為 h2。Emoji 代表問題的性質/主題，不代表嚴重度。嚴重度用分組標題或 metadata 行表達。選擇一眼能聯想到問題性質的 emoji。

---

## 定期掃描報告模板

### 設計原則

**3-30-300 Rule**：投影在週會上，成員有主管、Tech Lead、前後端工程師。

| 時間 | 讀者看到什麼 | 對應區塊 |
|------|------------|---------|
| 3 秒 | 有幾件事、多嚴重 | 「本次發現」severity-grouped bullet list |
| 30 秒 | 每個問題怎麼回事、建議做什麼 | 各 issue 的白話摘要 + 建議做法 |
| 300 秒 | 程式碼在哪、怎麼修 | `<details>` 收折的技術細節 |

**讀者分層**：
- **不展開任何東西**：主管、Tech Lead 得到完整決策資訊
- **展開「技術細節」**：工程師得到修復定位（code path、error flow、修改位置）
- **展開「查詢指令」**：需要驗證的人可重現所有數據

### 上次追蹤

偵測與時間範圍決定的流程在 SKILL.md 入口 A Step 0。此處僅定義輸出格式。

追蹤表每個項目標記狀態：
- ✅ **已改善**：error 消失或量體顯著下降（附本次數據佐證）
- 🔄 **仍存在**：error 仍在，量體無明顯變化
- ⚠️ **惡化**：量體上升
- ❓ **無法判斷**：本次掃描範圍未涵蓋、或需要人工確認

skill 只產出 data-based 的狀態，Jira 進度和實際處理狀態由團隊在週會上補充。

### 模板

```markdown
# {專案名稱} — {報告類型}

**{YYYY-MM-DD}** ｜ {profile} ｜ {起} ~ {迄} ｜ 掃描範圍：{簡述}

---

## 上次追蹤

> 前一份報告：{前一份報告檔名}（{YYYY-MM-DD}）
> 若為首次報告或使用者選擇跳過，此區塊省略。

| 項目 | 上次狀態 | 本次狀態 | 備註 |
|------|---------|---------|------|
| {上次行動項目} | {上次嚴重度} | ✅/🔄/⚠️/❓ | {數據變化或需人工確認的原因} |

---

## 本次發現

**嚴重問題** ({N})
- {主題emoji} {一句白話摘要，含量體}

**需要改進** ({N})
- {主題emoji} {一句白話摘要，含量體}

**Log 雜訊調整建議**
- {一句話概述可調整的 log 雜訊數量，無可調整項目則寫「本次無新增可調整雜訊」}

### error 每日趨勢

> {若前端多為後端反射，在此說明：「前端 error 多數為後端的反射，以後端數字為分級基準。」}

| 日期 | 後端 | 前端 | 備註 |
|------|------|------|------|
| {date} | {count} | {count} | {異常標記} |

---

## {主題emoji} {一句白話標題}

**{嚴重問題/需要改進}** ｜ {次數} ｜ {影響的系統層}

**怎麼回事**：{2-3 句白話，不熟悉系統的人也能理解。說明觸發條件、發生了什麼、為什麼。}

**影響**：{使用者感知的影響。「使用者不受影響」「查詢會變慢」「看到 500 錯誤頁」等白話描述。附量化指標（次數、佔比、與 baseline 的對比）。}

**建議做法**：

| 做法 | 說明 |
|------|------|
| {方案名} | {白話說明} |

<details>
<summary>🔧 技術細節</summary>

**錯誤流程**：

{ASCII flow diagram}

**Root Cause**：{file:line — 技術原因}

**關鍵證據**：
- {證據 1} [Q{n}]
- {證據 2} [S{n}]

**修改位置**：{具體 file path 和改法方向}

</details>

---

## ⚪ Log 雜訊調整建議

> 調整以下項目可降低後續掃描的 token 與查詢成本。

| 來源 | 項目 | 週量 | 建議調整 |
|------|------|------|----------|
| {log group path} | {項目} | {count} | {一句話建議工程師怎麼調，無建議則留空} |

---

## 行動清單

> 以下為基於技術調查的初步建議（Proposed），需團隊討論後定案。

| 優先 | 項目 | 建議負責方 | 對應 issue |
|------|------|-----------|-----------|
| 🔴 高 | {項目} | {團隊/角色，無法判斷則標 ❓} | {上方 issue 段落標題，如「👻 不存在的 profile…」} |
| 🟡 中 | {項目} | {團隊/角色} | {issue 段落標題} |
| ⚪ 低 | {項目} | {團隊/角色} | {issue 段落標題} |

---

## 附錄

<details>
<summary>系統架構與 Log 來源</summary>

{ASCII flow diagram}

{Log group 對照表}

</details>

<details>
<summary>查詢指令（可重現驗證）</summary>

**前置條件：** AWS Profile `{profile}`
**時間範圍：** `{start} ~ {end}`（epoch seconds）

[Q1]: **Q1 — {描述}** (recordsMatched: {n})
{完整 CLI 指令}

</details>
```

---

## 事件報告模板

事件報告基於 **BLUF（Bottom Line Up Front）** 原則與 **Inverted Pyramid（倒金字塔）** 結構，採用 **3-30-300 Rule** 分層的 **Progressive Disclosure** 讓不同角色在各自的時間預算內找到需要的資訊。

| 層次 | 區塊 | 目標讀者 | 閱讀時間預算 |
| --- | --- | --- | --- |
| Header blockquote | 異常時段 + 影響 + 處置 | 所有人 | 3 秒 |
| 30 秒掌握全貌 | Executive Card（表格化事件名片） | 主管、TPM——讀到這裡即可做決策 | 30 秒 |
| 事件總覽 | 發生什麼事 + 時序 + 根因 + Action Items | Tech Lead + 牽涉到的團隊（前端/後端/SRE等）——了解來龍去脈和分工 | 5 分鐘（300 秒） |
| 跨團隊影響與流程檢討 | 各角色重點 + 為什麼沒有更早發現 | Tech Lead + 需要跨團隊協調的角色 | 延伸閱讀 |
| 技術調查細節 | 指標圖表 + 次要因素 + 排除假設 + 驗證 + 查詢來源 | 牽涉到的工程團隊 | 完整技術脈絡 |

每一層都是前一層的展開——「30 秒掌握全貌」自成完整結論（BLUF），不需要往下看也能做決策。

引導讀者的方式是 **Inverted Pyramid 排列 + Register Shift（語彙轉換）**，不是標注「某角色讀到這裡即可」：
- **BLUF**：Header blockquote（3 秒）+ Executive Card（30 秒）讓讀者快速掌握結論
- **Register Shift**：Executive Card 和事件總覽用業務語言（影響範圍、處置結果）；技術調查細節用技術語言（指標、query、code path）——語彙轉換本身就是隱形閘門，非技術讀者自然停止
- **Visual separator**：`---` 分隔線標記層次邊界

調查過程的逐行 code reference、查證段落、完整查詢指令不放在報告裡——那是調查 log，不是報告。報告末尾附「查詢來源」表讓需要的人自行追溯即可。

```markdown
# {日期} {事件名稱}

> **異常時段**：{起} ~ {迄} TWN
> **影響**：{一句話描述影響範圍}
> **處置**：{已完成的緊急處置}
>
> 「30 秒掌握全貌」可快速了解事件結論與後續安排；「事件總覽」涵蓋完整來龍去脈與行動項目；後續章節為延伸分析與技術調查細節。

## 30 秒掌握全貌

| 項目 | 內容 |
| --- | --- |
| 🔴 嚴重度 | {🔴高/🟡中/⚪低}——{一句話說明為什麼是這個等級} |
| 💥 什麼事 | {一句話，用業務語言描述，不用技術名詞} |
| 👤 使用者影響 | {一句話：有感/無感/有限，附簡短說明} |
| 🔍 根本原因 | {一句話，用業務語言描述} |
| 🛠️ 目前狀況 | {已處理/觀察中/待修，附簡短說明} |
| 📋 待辦 | {統計數字：🔴 n 件（n 件已完成）/ 🟡 n 件 / ⚪ n 件} |
| 👥 負責 | {涉及的團隊} |

> ⬇️ 想了解完整來龍去脈，請繼續往下看。

---

## 事件總覽

### 發生什麼事

{用 bullet points 列出事件要點，每點一個事實——什麼觸發、核心機制、結果}
{包含使用者面向影響（透過互動階段取得）：使用者感受、影響範圍、客訴量等}
{本區塊不放圖表——保持純文字，讓非工程讀者快速掃描}

### 事件時序

{只列關鍵轉折點，用 code block 呈現}
{涵蓋完整生命週期：onset → peak → intervention（具體動作）→ recovery（指標回穩時間）}
{包含調查過程中的關鍵轉折：錯誤假設、排除方向、發現真因的時間點}

### 為什麼會這樣

{用表格呈現核心根因（通常 2-4 個），次要放大因素與排除假設放「技術調查細節」}

| # | 問題 | 發生了什麼 | 證據 |
| --- | --- | --- | --- |
| 1 | {一句話問題名稱} | {白話說明機制和影響} | `[來源編號]` |
| 2 | ... | ... | ... |

### Action Items（建議）

> 以下為基於技術調查的初步建議（Proposed），需團隊討論後定案。
> 已完成的緊急處理（如 hotfix）可在優先欄標示 ✅。

| 優先 | 項目 | 建議負責方 | 完成標準 | 對應 issue | 追蹤 |
|------|------|-----------|---------|-----------|------|
| 🔴 高 | {項目} | {團隊/角色，無法判斷則標 ❓} | {做到什麼算完成} | {「為什麼會這樣」表格中的問題編號} | {Jira 單號} |
| 🟡 中 | {項目} | {團隊/角色} | {完成標準} | {問題編號} | |
| ⚪ 低 | {項目} | {團隊/角色} | {完成標準} | {問題編號} | |

---

## 跨團隊影響與流程檢討

### 各角色需要了解的重點

> 依據事件實際牽涉的範圍增減角色。若為單一團隊內部事件且無跨領域影響，可直接省略此區塊。

| 角色 | 重點 |
| --- | --- |
| {受影響或需協作的角色 1} | {一句話說明該角色要知道的關鍵事實} |
| {受影響或需協作的角色 2} | {一句話說明該角色要知道的關鍵事實} |

### 為什麼沒有更早發現

{若有系統性根因（透過互動階段取得），用條列說明流程面原因}
{為什麼沒更早發現？既有流程/監控哪裡有缺口？是否為跨產品共通風險？}

---

## 技術調查細節

### 指標趨勢

{涉及基礎設施指標異常時，附 CloudWatch 圖表——描述放圖片下方 italic caption，不放 alt text（alt 渲染後不可見）}
![{short-name}](assets/{report-basename}/{service}-{metric}.png)
*{圖表要點描述} `[Qn]`*

{正常指標文字帶過即可，不需要附圖}

### 次要放大因素

{用表格精簡帶過}

### 排除的假設

{用表格呈現：假設、排除原因、來源}

| 假設 | 排除原因 | 來源 |
| --- | --- | --- |
| {我們懷疑的} | {為什麼排除了} | `[來源編號]` |

### 驗證方式

{成功標準表格 + 必要的驗證指令（精簡版）}

### 查詢來源

> 報告本文中的數據點用 [Q1]、[ALB-Q1] 等編號引用此表。
> 事件報告不需要附完整 CLI 指令，但每個數據都要能追溯到來源工具和查詢範圍。

| 編號 | 數據 | 工具 | 查詢範圍 |
| --- | --- | --- | --- |

---

## Changelog

> 新的在上（reverse chronological）。

| 日期 | 變更 |
| --- | --- |
| {YYYY-MM-DD} | {變更摘要} |
| {YYYY-MM-DD} | 初版：{一句話描述範圍} |
```

### 事件報告 vs 定期掃描報告的差異

| 面向 | 事件報告 | 定期掃描報告 |
| --- | --- | --- |
| 篇幅 | ~120-170 行（含 Executive Card） | 依問題數量而定 |
| 結構 | 四層 Progressive Disclosure（Executive Card → 總覽 → 影響與流程 → 技術細節） | 3-30-300（summary → 白話摘要 → 收折技術細節） |
| 技術細節 | 獨立章節（Register Shift 隔離） | `<details>` 收折在各 issue 內 |
| 查詢指令 | 只附來源表 | 附錄含完整可重現指令（收折） |
| 上次追蹤 | 無（單次事件） | 自動比對前一份報告的行動清單 |
| 目標 | 主管 30 秒掌握結論、Tech Lead 5 分鐘了解全貌、工程師讀完整脈絡 | 週會投影，全員 3 秒掌握概覽、30 秒理解問題、按需展開技術細節 |
